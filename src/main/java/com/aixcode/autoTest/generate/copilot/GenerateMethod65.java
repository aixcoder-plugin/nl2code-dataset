package com.aixcode.autoTest.generate.copilot;

import com.aixcode.autoTest.GenerateMethodBase;

import java.util.Date;

public class GenerateMethod65 extends GenerateMethodBase {
    /**
     * return the last day of the date's month of specified string value in format: yyyy-MM
     */
    public Date getMonthLastDay(String dateString) {
//        Date date = Date.parse(dateString);
//        return getMonthLastDay(date);
        return null;
    }
}
