package com.aixcode.autoTest.generate.copilot;

import com.aixcode.autoTest.GenerateMethodBase;

public class GenerateMethod172 extends GenerateMethodBase {
    /**
     * 十进制数值减法，字符串表示每一个数
     */
    public String bigDecimalSubtract(String str, String str1) {
        String result = "";
        int len = str.length();
        int len1 = str1.length();
        int len2 = len > len1 ? len : len1;
        int len3 = len > len1 ? len1 : len;
        int len4 = len2 - len3;
        int len5 = len4 > 0 ? len4 : 0;
        int len6 = len2 - len5;
        int len7 = len6 > 0 ? len6 : 0;
        int len8 = len2 - len7;
        int len9 = len8 > 0 ? len8 : 0;
        int len10 = len2 - len9;
        int len11 = len10 > 0 ? len10 : 0;
        int len12 = len2 - len11;
        int len13 = len12 > 0 ? len12 : 0;
        int len14 = len2 - len13;
        int len15 = len14 > 0 ? len14 : 0;
        int len16 = len2 - len15;
        int len17 = len16 > 0 ? len16 : 0;
        int len18 = len2 - len17;
        int len19 = len18 > 0 ? len18 : 0;
        int len20 = len2 - len19;
        int len21 = len20 > 0 ? len20 : 0;
        int len22 = len2 - len21;
        int len23 = len22 > 0 ? len22 : 0;
        int len24 = len2 - len23;
        int len25 = len24 > 0 ? len24 : 0;
        int len26 = len2 - len25;
        int len27 = len26 > 0 ? len26 : 0;
        int len28 = len2 - len27;
        int len29 = len28 > 0 ? len28 : 0;
        int len30 = len2 - len29;
        int len31 = len30 > 0 ? len30 : 0;
        int len32 = len2 - len31;
        int len33 = len32 > 0 ? len32 : 0;
        int len34 = len2 - len33;
        return result;
    }
}
