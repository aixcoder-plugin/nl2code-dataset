package com.aixcode.autoTest.version1.aixcoder;

public class Aixcoder387 {
    /**
     * 判断是否只含字母
     */
    public static boolean isLetter(char c) {
        if (c >= 'a' && c <= 'z')
            return true;
        else
            return false;
    }
}
