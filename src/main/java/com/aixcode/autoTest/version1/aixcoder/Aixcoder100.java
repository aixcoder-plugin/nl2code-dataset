package com.aixcode.autoTest.version1.aixcoder;

public class Aixcoder100 {
    /**
     * Generate a hex string for the specified bytes.
     */
    public static final String toHex(byte[] byteArr) {
        if (byteArr == null)
            return"";

//        return toHex(byteArr, byteArr.length);
        return  null;
    }

}
