package com.aixcode.autoTest.aixcoderV3;

import com.aixcode.autoTest.GenerateMethodBase;

public class AixcoderNew18 extends GenerateMethodBase {
    public byte[] readBytes(java.io.InputStream in, int len)
            throws java.io.IOException {
//        if (!useWrap) {
//            return inStream.readBytes(in, len);
//        }
//        if (ostart >= ofinish) {
//// we loop for new data as we are blocking
//            int i = 0;
//            while (i == 0) i = readMoreData(in);
//            if (i == -1)
//                throw new IOException("EOF with " + len + " bytes remaining");
//        }
//        if (len <= 0) {
//            return new byte[0];
//        }
//        byte[] bytes = new byte[len];
//        System.arraycopy(obuffer, ostart, bytes, 0, len);
//        ostart = ostart + len;
//        return bytes;
        return null;
    }
}
